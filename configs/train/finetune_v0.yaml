features_path: artifacts/features/features_v0.parquet
foundation_checkpoint: artifacts/checkpoints/foundation_v0/best.pt
output_dir: artifacts/checkpoints/finetune_v0
subset:
  - AAPL
  - MSFT
  - AMZN
batch_size: 128
epochs: 25
learning_rate: 0.0001
model:
  name: mlp
  hidden_sizes: [128, 128]
  identity_embeddings: true
utility:
  name: semivariance
constraint:
  name: long_only_simplex
